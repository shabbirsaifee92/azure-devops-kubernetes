name: PR

on:
  pull_request:
    types: [ labeled ]

jobs:
  build:
    if: ${{ contains(github.event.label.name, 'integration') }}
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: echo Hello, world!
